# Build Resources

Place your app icons here:

- `icon.ico` — Windows (256x256 minimum, multi-size recommended)
- `icon.icns` — macOS (512x512)
- `icon.png` — Linux (512x512)

electron-builder uses these during packaging. If missing, the default Electron icon is used.
